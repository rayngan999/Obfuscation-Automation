if((self.parent&&!(self.parent===self))&&(self.parent.frames.length!=0)){
//self.parent.location=document.location
};
jQuery(function(){
	// var initialWidth = jQuery('header input#s').css('width');
	// jQuery('header input#s').focus(function(){jQuery(this).css({'width':560})});
	// jQuery('header input#s').blur(function(){jQuery(this).css({'width':initialWidth})});

	//jQuery().UItoTop({scrollSpeed: 600});
	// jQuery('.ctc-tag-cloud li').each(function(){$(this).attr('value', parseFloat($('a',this).css('font-size'))*100); });
	// jQuery('.ctc-tag-cloud').tagcloud({sizemin:10, sizemax:16, colormin:"006084", colormax:'113F50', type:'list'});
	// function determineHeight(){
		// var tallest = 0;
		// jQuery('footer.journalArticles section').each(function() {var thisHeight = $(this).height();if(thisHeight > tallest) {tallest = thisHeight;}});
		// jQuery('footer.journalArticles section div.wrapper').height(tallest - 30);
	// };
	// determineHeight();
	// jQuery('window').bind('resize', function(){
		// determineHeight();
	// });	
	

	// if (jQuery('#nav-wrapper').length){
		// var nav = jQuery('#nav-wrapper').detach();
		
		// jQuery('#container').append(nav);
		
		// jQuery(window).resize(function() {
		 // /*
			// if (jQuery(window).width() > 1266){
				// if ((jQuery('#nav-wrapper').css('display') == 'none')||(jQuery('#nav-wrapper').css('opacity') == 0)){
				  // jQuery('#nav-wrapper').css({'opacity': 0});
				  // jQuery('#nav-wrapper').css({'display':'block'}).animate({'opacity':1}, 1600); 
				// }
			// } else {
				// jQuery('#nav-wrapper').css({'display':'none'});
			// }
		// */
		// });
		 
		// var a = setTimeout(function(){
			  // jQuery(window).triggerHandler('resize');
		// }, 10000);
		// jQuery(window).scroll(function() {
			// //jQuery(window).triggerHandler('resize');
			// clearTimeout(a);
		// });	
		
		// jQuery('span.nav-next a, span.nav-previous a').click(function(e){
			// e.preventDefault();
			// var that = jQuery(this);
			// jQuery('#content').wrap('<div class="content-wrapper"></div>');
			// jQuery('#container').css({'overflow':'hidden'});
			// var direction;
			// if (that.parents('span').hasClass('nav-next'))
				// direction = 1;
			// else
				// direction = -1;
			// jQuery('div.content-wrapper').animate({'margin-left': direction * '990' + 'px', 'opacity':0}, 600, function(){
				// document.location = that.attr('href');
			// });
		
		// });		
	// }

});
